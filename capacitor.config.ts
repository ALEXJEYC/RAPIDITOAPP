import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.rapidito.app',
  appName: 'RAPIDITO',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
